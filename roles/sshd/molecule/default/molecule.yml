---
scenario:
  name: default
dependency:
  name: galaxy
  enabled: false
driver:
  name: docker
platforms:
  - name: debian11
    image: jrei/systemd-debian:11
    command: /lib/systemd/systemd
    tmpfs:
      - /run
      - /tmp
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
provisioner:
  name: ansible
verifier:
  name: ansible
lint: |
  yamllint .
  ansible-lint
  flake8
